import 'package:flutter/material.dart';

class UpComing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upcoming feature'),
      ),body: Center(child: Text("It's empty for now 🌚🌚"),),
    );
  }
}
